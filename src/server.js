const express = require('express');

// Inicializaciones
const app = express();

// Configuraciones
app.set('port', process.env.PORT || 3000);

// Middlewares

// Variables globales

// Rutas

// Archivos estaticos

module.exports = app;